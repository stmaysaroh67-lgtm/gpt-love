{
    "name": "AI Stealth Gateway",
    "image": "mcr.microsoft.com/devcontainers/javascript-node:18",
    
    "hostRequirements": {
        "cpus": 4,
        "memory": "16gb"
    },
    
    "forwardPorts": [3001],
    "portsAttributes": {
        "3001": {
            "label": "API Gateway",
            "visibility": "public"
        }
    },
    
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y xvfb libgbm-dev libnss3 libasound2 libatk-bridge2.0-0 libgtk-3-0",
    
    "postStartCommand": "npm install && sudo npm install -g pm2 && pm2 delete all || true && pm2 start gateway.js --name ai-gateway",
    
    "customizations": {
        "vscode": {
            "settings": {},
            "extensions": []
        }
    }
}
