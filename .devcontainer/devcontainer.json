{
    "name": "AI Stealth Gateway",
    
    "hostRequirements": {
        "cpus": 4,
        "memory": "16gb"
    },
    
    "forwardPorts": [3001],
    "portsAttributes": {
        "3001": {
            "label": "API Gateway",
            "visibility": "public"
        }
    },
    
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y xvfb libgl1 libglx-mesa0 libosmesa6 libgbm-dev libnss3 libasound2 libatk-bridge2.0-0 libgtk-3-0 && npm install && echo 'if ! npx pm2 status | grep -q \"ai-gateway\"; then cd /workspaces/* && npx pm2 start npm --name \"ai-gateway\" -- run start; fi' >> ~/.bashrc"
}
